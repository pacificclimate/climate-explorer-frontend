import PropTypes from 'prop-types';
import React from 'react';

import _ from 'underscore';


export default class RasterControls extends React.Component {
  static propTypes = {
  };

  constructor(props) {
    super(props);

    this.state = {
    };
  }

  render() {
    return (
      <div>RasterControls</div>
    );
  }
}
